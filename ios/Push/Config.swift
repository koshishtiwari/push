// Fill these in after deploying the server to Railway.
// API_KEY must match the API_KEY env var on the server.
enum Config {
    static let serverURL = "https://YOUR_APP.up.railway.app"
    static let apiKey   = "YOUR_API_KEY"
}
